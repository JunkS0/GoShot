services:
  - type: web
    name: kotgun-multiplayer-server
    env: node
    plan: free
    buildCommand: yarn install
    startCommand: yarn start
    envVars:
      - key: NODE_VERSION
        value: 20
